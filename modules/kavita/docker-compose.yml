services:
  kavita:
    image: jvmilazz0/kavita:latest
    container_name: kavita
    ports:
      - "5000:5000"
    volumes:
      - /data/manga:/manga
      - /data/config:/kavita/config
    restart: unless-stopped
